# Harris Automations — Proposals

Public GitHub Pages host for sales proposals.

Each proposal lives at `/{slug}/index.html` and renders at `https://harrisautomations.github.io/harris-proposals/{slug}/`.

Do not edit files here directly — proposals are deployed by `execution/deploy_web_proposal.py`.
